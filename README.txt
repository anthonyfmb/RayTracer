The executable is in x64/Release/
In that same directory is an example input file (txt) and example output (png)

Usage: RayTracer.exe [input file] [output file]

It usually takes a minute or so to generate the output, if there are any issues try rebuilding the project in Visual Studio